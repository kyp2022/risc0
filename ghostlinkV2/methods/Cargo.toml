[package]
name = "ghostlink-v2-methods"
version.workspace = true
edition.workspace = true

[build-dependencies]
risc0-build = { path = "../../risc0/build" }

[package.metadata.risc0]
methods = ["guest"]
